# HANDOFF — Blog Approval Discord Delivery (per-article HTMLs not arriving)

**Date:** 2026-07-09
**From:** KENSEI (Hermes) session
**System:** Hermes Agent cron `blog-approval-requester` (job_id `93154992274d`) → Discord `#blog-management` (channel id `1521172577031028888`)
**Repo:** `/home/kensei/repos/KenseiAgent` (branch `main`)
**Gateway:** systemd `hermes-gateway.service`, runs from `/home/kensei/repos/KenseiAgent` venv; gateway.py / scheduler.py loaded from THIS repo, NOT the `hermes-agent` fork.

---

## 1. WHAT THE USER ACTUALLY WANTS

Two distinct outputs per cron run:
1. **A summary/status HTML** — title list, counts, per-item `!approve`/`!reject`. (This works — user sees it.)
2. **One openable HTML file per article, with embedded images.** 10 blog previews + 16 X/Twitter previews = 26 files. Each is generated by `blog_approval._generate_preview()` into `/home/kensei/.hermes/reports/blog-previews/<slug>.html` (250–670 KB each, base64 images embedded, all <8 MB).

The user confirmed the 20:55 batch format (separate openable HTMLs, images intact) is "PERFECT". He does NOT want a single inlined blob (that blew to 13.6 MB and Discord rejected it). He also floated a future enhancement: a single tabbed HTML with all articles + images — but that is NOT the current ask.

## 2. CURRENT SYMPTOM (verified by user, 2026-07-09 evening)

User receives ONLY the summary index HTML in #blog-management. **None of the 26 article HTMLs arrive.** The cron run logs `delivered to discord:1521172577031028888` with success and zero error/429 lines — so the failure is SILENT (no log evidence), which is why past "verification" was misleading.

## 3. WHAT I CHANGED (and why it didn't fix it)

### Change A — digest generator (WRONG TURN, already corrected)
`content_engine/scripts/build_pending_digest.py` now builds a lightweight **status index only** (no inlined bodies, no images). Cron prompt (`/home/kensei/.hermes/cron/jobs.json`) updated to emit: summary text + `MEDIA:<index>` + one `MEDIA:` per article preview. This part is correct and matches the user's desired format.

### Change B — Discord media delivery batching (THE FIX ATTEMPT)
**File:** `plugins/platforms/discord/adapter.py`, function `_standalone_send` (the cron's actual delivery path via `tools/send_message_tool._send_to_platform` → registry `standalone_sender_fn`).

**Original bug:** it looped `for media_path in media_files: session.post(url, files[0]=...)` — one API POST per file, no rate-limit spacing. 26 rapid POSTs → Discord burst-429 → excess dropped silently.

**My fix:** batch into ≤10 files per message, `await asyncio.sleep(1.0)` between batches, 429-retry-once inline, added an INFO log on successful batch. Also added a parallel `send_multiple_documents` method + rewrote `_send_media_via_adapter` in `cron/scheduler.py` (batches docs, sends voice/image/video individually) — but NOTE: the cron path uses `_standalone_send`, NOT the in-process `send()`/`send_multiple_documents`, so the scheduler.py change may be dead for this flow.

**Tests added (pass):**
- `tests/cron/test_standalone_discord_batching.py` — mocks aiohttp, asserts 26 files → 3 posts (10+10+6), each ≤10.
- `tests/cron/test_scheduler_doc_batching.py` — unit test of `send_multiple_documents` chunking.
- `tests/cron/test_scheduler_media_coercion.py` — pre-existing, still passes.

**Result: STILL BROKEN after this fix.** User confirms only the index arrives. So either (a) `_standalone_send` isn't the live path, or (b) there's another cap I haven't found.

## 4. OPEN HYPOTHESES THE NEXT AGENT MUST TEST

1. **Live in-process adapter path.** `_deliver_result` (scheduler.py ~2010) calls `_send_media_via_adapter(runtime_adapter, ...)` IF `adapter_ok` (a live gateway adapter is available). The cron may run INSIDE the gateway process, making `runtime_adapter` live → it uses `DiscordAdapter.send()` / `send_multiple_documents`, NOT `_standalone_send`. My `send_multiple_documents` patch is UNTESTED against the real `_send_media_via_adapter` flow and may not be triggered. **Need to confirm which path the cron actually takes** (grep `adapter_ok` / `_send_media_via_adapter` call site; check if cron scheduler runs in the gateway process or a separate `hermes cron` process).

2. **`filter_media_delivery_paths` silently dropping paths.** It calls `validate_media_delivery_path`. All 26 paths are under `~/.hermes/reports/blog-previews/` which should be in `MEDIA_DELIVERY_SAFE_ROOTS` (verified `validate_media_delivery_path` returns the path for the index). But confirm it returns non-None for the per-article slugs too — a subtle root mismatch would drop them. **Add a log line printing `len(media_files)` after filter at scheduler.py ~1667.**

3. **Discord message-rate / burst cap beyond per-message 10.** Even with 1s between batches, Discord may rate-limit the *number of messages* from a bot in a short window. The 20:55 batch (6 files) worked; 26 (3 messages) didn't. Test: does sending 3 messages 1s apart actually deliver all 3? May need larger delay or a single combined approach.

4. **The LLM response itself.** The cron output file shows 26 MEDIA tags, but `_deliver_result` operates on `final_response` AFTER the scheduler's raw-HTML strip + truncation. If the truncation block (scheduler.py ~3885) or raw-HTML strip (wrongly) eats MEDIA lines when there are many, only the index survives. **Log `len(media_files)` and the raw `final_response` length right before delivery.**

## 5. SIDE ISSUE — ORPHAN GATEWAYS

Every `systemctl restart hermes-gateway` spawns **10–12 orphan `gateway run --replace` processes** (PIDs observed 2545932–, 3325770–, 3437148-era). They compete for the same Discord connection and may interfere with delivery. Kill with explicit PIDs (NOT bare `pkill -9 -f "gateway run --replace"` — it killed the shell's own process group once). After fix, verify no orphans return on restart. Root cause of the respawn is unknown — likely a `Restart=always` or `--replace` self-respawn in the gateway launcher.

## 6. KEY FILES / COMMANDS

- Cron config: `/home/kensei/.hermes/cron/jobs.json` (job `blog-approval-requester`, prompt hardened)
- Per-article previews: `/home/kensei/.hermes/reports/blog-previews/*.html` (regenerate via `cd /home/kensei/repos/KenseiAgent/content_engine && PYTHONPATH=. ../.venv/bin/python scripts/build_pending_digest.py`)
- Pending tracker: `content_engine/blog_topics/pending_approvals.jsonl`
- Delivery code: `plugins/platforms/discord/adapter.py` (`_standalone_send` ~9179, `send_multiple_documents` ~2921), `cron/scheduler.py` (`_deliver_result` ~1566, `_send_media_via_adapter` ~1414, truncation ~3885), `tools/send_message_tool.py` (`_send_to_platform` ~734)
- Live test: `cronjob action=run job_id=93154992274d` → watch `/home/kensei/.hermes/logs/agent.log` for `delivered to discord` + any `media`/`429`/`batch` ERROR/WARNING.
- Gateway restart: `sudo systemctl restart hermes-gateway` (then kill orphans).

## 7. WHAT "DONE" LOOKS LIKE

User opens #blog-management after a cron run and sees: the summary index + 26 article HTMLs as native Discord file attachments (grouped or individual), each openable with its embedded images. No code changes are "done" until the user visually confirms the 26 files landed — log success alone is NOT sufficient proof (proven by this session).

## 8. COMMIT STATE

All changes are UNCOMMITTED in `/home/kensei/repos/KenseiAgent` (branch `main`). Do NOT commit until the fix is visually confirmed by the user (standing rule: explicit permission required before commits/state-changing git ops).
